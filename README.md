# SvelteKit Reproduction: Can't assign result of `preloadData` to local state after using `pushState`

Reproduction for https://github.com/sveltejs/kit/issues/14960.

## Installation

```sh
pnpm i
pnpm run dev --open
```

Then follow the steps and description in the app itself.
